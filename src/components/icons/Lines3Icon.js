import React from "react";
import { ReactComponent as Lines3 } from '../assets/3lines.svg'; 

const Lines3Icon = ({width, height, fill, className}) => {
    return <Lines3 width={width} height={height} fill={fill} className={className} />
}

export default Lines3Icon;
