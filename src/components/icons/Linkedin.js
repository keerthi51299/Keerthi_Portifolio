import React from "react";
import { ReactComponent as Linkedin } from '../assets/linkedin.svg'; 

const LinkedinIcon = ({width, height, fill, className}) => {
    return <Linkedin width={width} height={height} fill={fill} className={className} />
}

export default LinkedinIcon;
